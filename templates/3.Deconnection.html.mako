<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" type="text/css" href="${url_for('static', filename='style.css')}">
        <meta name="author" content="Moi-png, ELPHILA">
        <title>Formulaire de déconnection</title>
    </head>
    <body>
        <div class="center">
            <a class="Gauche" href="${url_for('index')}"><img src="${url_for('static', filename='7Goodbye.png')}" alt="plage" width="" height=""></a>
        </div>
    </body>
</html>